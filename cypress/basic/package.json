{
  "name": "basic",
  "private": true,
  "scripts": {
    "test": "cypress run --headed --no-exit --browser=chrome"
  },
  "devDependencies": {
    "@axe-core/watcher": "../../../jazzband/integrations/javascript/dist",
    "cypress": "^13.13.0"
  }
}
