{
  "name": "basic",
  "private": true,
  "scripts": {
    "test": "cypress run --headed --browser=chrome"
  },
  "devDependencies": {
    "@axe-core/watcher": "^3.22.0",
    "cypress": "^13.13.0"
  }
}
